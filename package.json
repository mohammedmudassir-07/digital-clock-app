{
  "name": "digitalclock",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --open=./index.html --port=3000",
    "start": "npm run dev"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
